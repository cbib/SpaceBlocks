# CORE sample sheet — technology-agnostic anchor (see config.yaml: core_samples).
# Required column: sample. Optional columns (here: patient, type) are stamped into
# obs by preprocess_umap and surfaced downstream via config.extra_annotations.
# `sample` values match config/samples.csv sample_id (visiumhd head run).
# IMPORTANT: patients 1 3 5 and 7 are shared with the HTGEdgeSeq study and match
# their patient id there; the remaining patients are not shared between studies.
# Lines starting with '#' are ignored (no commas inside so no quoting).
sample	patient	batch
sample1	Patient 1	Batch 1
sample2	Patient 2	Batch 2
sample3	Patient 1	Batch 1
